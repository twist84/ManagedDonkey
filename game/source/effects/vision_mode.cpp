#include "effects/vision_mode.hpp"

void s_vision_mode_definition::update_reference_names()
{
	UPDATE_REFERENCE_NAME(ping_start_sound);
	UPDATE_REFERENCE_NAME(mask_bitmap);
	UPDATE_REFERENCE_NAME(camera_fx_overlay);
}

